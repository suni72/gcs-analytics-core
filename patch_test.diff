<<<<<<< SEARCH
  @Test
  void testGetFolderMetadata_unimplemented() {
    assertThrows(
        UnsupportedOperationException.class,
        () -> gcsClient.getFolderMetadata(GcsItemId.builder().setBucketName("test-bucket").build()));
  }
}
=======
  @Test
  void testGetFolderMetadata_unimplemented() {
    assertThrows(
        UnsupportedOperationException.class,
        () -> gcsClient.getFolderMetadata(GcsItemId.builder().setBucketName("test-bucket").build()));
  }

  @Test
  void listObjects_returnsItemsUpToMaxResults() throws IOException {
    String bucketName = "test-bucket";
    String prefix = "test-prefix/";
    storage.create(BlobInfo.newBuilder(bucketName, prefix + "file1.txt").build());
    storage.create(BlobInfo.newBuilder(bucketName, prefix + "file2.txt").build());
    storage.create(BlobInfo.newBuilder(bucketName, prefix + "file3.txt").build());

    GcsItemId dirId = GcsItemId.builder().setBucketName(bucketName).setObjectName(prefix).build();
    List<GcsItemInfo> results = gcsClient.listObjects(dirId, 2);

    assertThat(results).hasSize(2);
    assertThat(results.get(0).getItemId().getBucketName()).isEqualTo(bucketName);
    assertThat(results.get(1).getItemId().getBucketName()).isEqualTo(bucketName);
  }

  @Test
  void listObjects_withEmptyPrefix_listsBucketRoot() throws IOException {
    String bucketName = "test-root-bucket";
    storage.create(BlobInfo.newBuilder(bucketName, "fileA.txt").build());
    storage.create(BlobInfo.newBuilder(bucketName, "fileB.txt").build());

    GcsItemId bucketId = GcsItemId.builder().setBucketName(bucketName).build();
    List<GcsItemInfo> results = gcsClient.listObjects(bucketId, 5);

    assertThat(results).hasSize(2);
  }

  @Test
  void listObjects_withStorageException_throwsIOException() throws IOException {
    GcsItemId dirId = GcsItemId.builder().setBucketName("error-bucket").setObjectName("test/").build();
    org.mockito.Mockito.doThrow(new StorageException(500, "Internal Server Error"))
        .when(storage).list(org.mockito.ArgumentMatchers.eq("error-bucket"), org.mockito.ArgumentMatchers.any());

    IOException exception = assertThrows(IOException.class, () -> gcsClient.listObjects(dirId, 1));
    assertThat(exception.getMessage()).contains("Unable to list objects for");
  }
}
>>>>>>> REPLACE
